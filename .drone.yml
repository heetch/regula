workspace:
  base: /go
  path: src/github.com/heetch/rules-engine

pipeline:
  build:
    image: golang:1.9-alpine
    commands:
      - apk --update add git glide
      - glide --quiet install
      - go test -v ./...
    when:
      event: push

services:
  database:
    image: consul:latest
